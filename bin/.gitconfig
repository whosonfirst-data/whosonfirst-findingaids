# https://www.git-scm.com/book/en/v2/Customizing-Git-Git-Configuration

[user]
	name = whosonfirst-bot
	email = devnull@localhost

[core]
	editor = emacs
[color]
	ui = false

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
